spring:
  datasource:
    url: jdbc:h2:file:/Users/raccon/개인공부/DB/jpashop
    username : sa
    password:
    driver-class-name: org.h2.Driver


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true


logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.orm.jdbc.bind: trace